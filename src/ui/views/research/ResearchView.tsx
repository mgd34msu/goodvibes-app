// Slot module — the Wave C agent that owns views/research/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function ResearchView(): React.ReactElement {
  return <ComingSoon title="Research" wave="Wave C" />;
}
