// Slot module — the Wave B agent that owns views/providers/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function ProvidersView(): React.ReactElement {
  return <ComingSoon title="Providers & Models" wave="Wave B" />;
}
