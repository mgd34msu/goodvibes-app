// Slot module — the Wave B agent that owns views/watchers/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function WatchersView(): React.ReactElement {
  return <ComingSoon title="Watchers" wave="Wave B" />;
}
