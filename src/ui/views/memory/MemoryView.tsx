// Slot module — the Wave B agent that owns views/memory/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function MemoryView(): React.ReactElement {
  return <ComingSoon title="Memory" wave="Wave B" />;
}
