// Slot module — the Wave B agent that owns views/channels/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function ChannelsView(): React.ReactElement {
  return <ComingSoon title="Channels" wave="Wave B" />;
}
