// Slot module — the Wave B agent that owns views/code/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function WorktreesView(): React.ReactElement {
  return <ComingSoon title="Worktrees" wave="Wave B" />;
}
