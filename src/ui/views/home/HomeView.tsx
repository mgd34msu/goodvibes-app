// Slot module — the Wave C agent that owns views/home/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function HomeView(): React.ReactElement {
  return <ComingSoon title="Home" wave="Wave C" />;
}
