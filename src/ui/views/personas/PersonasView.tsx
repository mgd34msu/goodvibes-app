// Slot module — the Wave C agent that owns views/personas/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function PersonasView(): React.ReactElement {
  return <ComingSoon title="Personas" wave="Wave C" />;
}
