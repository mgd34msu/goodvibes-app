// Slot module — the Wave D agent that owns views/observability/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function ObservabilityView(): React.ReactElement {
  return <ComingSoon title="Observability" wave="Wave D" />;
}
