// Slot module — the Wave C agent that owns views/routines/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function RoutinesView(): React.ReactElement {
  return <ComingSoon title="Routines" wave="Wave C" />;
}
