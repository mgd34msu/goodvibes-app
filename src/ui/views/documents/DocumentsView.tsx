// Slot module — the Wave C agent that owns views/documents/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function DocumentsView(): React.ReactElement {
  return <ComingSoon title="Documents" wave="Wave C" />;
}
