// Slot module — the Wave B agent that owns views/automation/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function AutomationView(): React.ReactElement {
  return <ComingSoon title="Automation" wave="Wave B" />;
}
