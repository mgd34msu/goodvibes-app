// Slot module — the Wave B agent that owns views/knowledge/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function KnowledgeView(): React.ReactElement {
  return <ComingSoon title="Knowledge" wave="Wave B" />;
}
