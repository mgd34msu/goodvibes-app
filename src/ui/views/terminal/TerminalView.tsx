// Slot module — the Wave D agent that owns views/terminal/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function TerminalView(): React.ReactElement {
  return <ComingSoon title="Terminal" wave="Wave D" />;
}
