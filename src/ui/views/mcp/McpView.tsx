// Slot module — the Wave B agent that owns views/mcp/ replaces this
// stub entirely. Registry + palette wiring already point here; do not rename.
import { ComingSoon } from "../ComingSoon.tsx";

export function McpView(): React.ReactElement {
  return <ComingSoon title="MCP" wave="Wave B" />;
}
