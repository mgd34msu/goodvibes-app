# deps / build
node_modules/
build/
# Electrobun build output at the repo root ONLY — an unanchored "artifacts/"
# silently ignored src/ui/views/artifacts/ (the Artifacts view source).
/artifacts/
dist/
*.tsbuildinfo

# plugin/session state (not app code)
.goodvibes/

# logs & local scratch
*.log
tmp/
